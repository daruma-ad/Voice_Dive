# 個人開発の記録：AI音声面接プラットフォーム「VoiceDive」を開発した道のり

こんにちは。今回は、個人開発で取り組んできた**「VoiceDive（ボイスダイブ）」**というAI音声面接プラットフォームの開発プロセスについて、技術的な振り返りも兼ねてブログ形式でまとめてみようと思います。

## VoiceDiveとは？
VoiceDiveは、ブラウザだけで気軽に本格的な面接のロールプレイが行えるWebアプリケーションです。
主に接客業（カフェ店員やホールスタッフなど）を想定し、**ユーザーがマイクに向かって話した言葉をAIが理解し、リアルタイムに会話のキャッチボールを行う**という体験を提供します。

最大の特徴は、面接終了後にAIが会話内容を分析し、「流暢さ」「敬語」「ホスピタリティ」などの項目でスコア化してくれる点です。練習モードも備わっており、プレッシャーを感じずに何度でも挑戦できます。

## 採用した技術スタック
今回のプロジェクトでは、フロントエンドの快適さとバックエンドのAI処理能力の両立を目指して、以下の構成を採用しました。

*   **フロントエンド:** Next.js (App Router), React, Tailwind CSS
*   **バックエンド:** FastAPI (Python)
*   **AIモデル:** Google AI Studio (Gemini 2.0 Flash)
*   **音声認識:** Web Speech API (ブラウザ標準機能)
*   **データベース:** Firebase / Firestore
*   **ホスティング:** Vercel (FE), Render (BE)

## 開発のフェーズと直面した課題

### フェーズ1：プロジェクトの立ち上げと静的UIの構築
まずはユーザー体験（UX）の基礎となる画面設計からスタートしました。
Viteを使ってランディングページ（LP）や面接のフロー（履歴書アップロード → マイクテスト → 面接セッション → 結果画面）のモックアップを作成しました。
この段階では綺麗なUIを作ることに注力し、ガラスモーフィズムを取り入れたモダンなデザインに仕上げました。

### フェーズ2：Next.jsへの移行とバックエンドの構築
当初の静的サイトから、より本格的なWebアプリケーションに進化させるため、**Next.js (App Router)** に移行しました。
同時に、Pythonで**FastAPI**を使用したバックエンドサーバーを構築。ここでGemini APIと連携し、履歴書の解析やAIの思考ロジック部分を作り込みました。

### フェーズ3：リアルタイム音声認識との格闘（最大の壁）
最も苦労したのが、ブラウザ標準の **Web Speech API** を使った音声認識の実装です。
PCのChromeでは比較的スムーズに動いても、**モバイル端末（特にAndroid）**になると途端に挙動が不安定になりました。

*   **問題1：文字が何度も重複して入力されてしまう**
    *   APIの仕様上、過去に認識した文字列が新しい認識結果に累積されて返ってくることがあり、ReactのStateと噛み合わずに「こんにちはこんにちは」と連続して表示されてしまうバグに遭遇しました。
    *   **解決策:** `transcript` の累積状態を独自のロジックで差分計算し、新しく認識された文字列のみを抽出して追加していくフック（`useSpeech.ts`）を実装することで解決しました。
*   **問題2：無言の間ができると勝手にマイクが切れる**
    *   これもAPIの仕様でした。
    *   **解決策:** ユーザーが「面接終了」ボタンを押すまでは、システムが自動的にマイクを再起動し続けるように制御を入れました。

### フェーズ4：AIとの会話のレイテンシ改善
面接のロールプレイを自然に行うため、ユーザーが話し終えてからAIが返答するまでの間隔（レスポンスタイム）を極力短くする必要がありました。
システムプロンプトの調整や、フロントエンド側での「AI考え中...」のUI表示を工夫することで、待たされている感覚を軽減しました。

### フェーズ5：システムへの保存と「練習モード」の誕生
面接が終わった後、結果をFirebase (Firestore) に保存する機能を実装しました。
しかし、「保存処理」自体が通信環境によって長引くことがあり、完了画面でユーザーを待たせてしまう事態が発生しました。
そこで、**保存処理をバックグラウンドに回し、万が一送れなくても3秒でタイムアウトさせて画面遷移を優先する非同期UX**へと切り替えました。

また、開発を進める中で**「ちょっと試したいだけなのに、毎回データが残るのはプレッシャーだな...」**と感じ、本番モードとは別に、データベースに一切送信しない**「練習モード」**を急遽実装したのも良い思い出です。

## さいごに
AIを使った音声会話のWebアプリ開発は、ブラウザの仕様や非同期処理、プロンプトエンジニアリングなど、想像以上に多くの領域の知識が求められました。
しかし、実際に自分が喋った言葉に対してAIが相槌を打ってくれたり、鋭い質問を返してきたりする体験がブラウザ上で動いた時は、大きな感動がありました。

今回は主に「接客業の面接」というテーマで作成しましたが、この基盤を応用すれば、英会話の練習アプリや、ロールプレイ型の研修システムなど、様々なサービスに応用できそうです。

---
*この記事はVoiceDiveの開発記録として作成されました。*
